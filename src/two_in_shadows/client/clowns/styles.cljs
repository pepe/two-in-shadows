(ns two-in-shadows.client.clowns.styles)


(defonce ^:const line {:display "flex" :align-items "center"
                       :justify-content "space-between"
                       :padding-left "1rem" :height "2rem"
                       :border-radius "1rem"})

(defonce ^:const listing {:padding 0 :width "28rem"})


(defonce ^:const form {:width "28rem" :border-radius "1rem" :margin "1rem 0"})

(defonce ^:const form-title {:text-align :center :margin "0.5rem"})
